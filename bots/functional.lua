function toList( ... )
	if select("#", ...) == 1 and type(...) == "table" then
		return ...
	else
		return {...}
	end
end

function init( ships, world, gfx )

end

function run( enemyShips, sensors )

end